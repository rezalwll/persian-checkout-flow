{
  "name": "component-patterns",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=14"
  },
  "scripts": {
    "lint": "node scripts/lint.mjs",
    "build": "tsc -p tsconfig.json",
    "test": "npm run build --silent && node scripts/test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "18.0.0",
    "react-dom": "18.0.0"
  },
  "devDependencies": {
    "@types/react": "18.0.0",
    "@types/react-dom": "18.0.0",
    "typescript": "4.6.3"
  }
}
